# YouTube Video Downloader

CLI-утилита на Python для скачивания видео с YouTube с выбором качества.

## Возможности

- 🎥 Скачивание видео с YouTube в формате MP4
- 🎯 Выбор качества видео (1080p, 720p, 480p и др.)
- 💻 Интерактивный режим или использование через параметры командной строки
- 📊 Отображение прогресса загрузки
- ⚙️ Настройка директории для сохранения через конфигурационный файл

## Технологии

- **Python** 3.11+
- **uv** — управление зависимостями
- **yt-dlp** — библиотека для загрузки видео
- **python-dotenv** — управление конфигурацией

## Установка

### Требования

- Python 3.11 или выше
- [uv](https://docs.astral.sh/uv/) — установите командой:

  ```bash
  # Windows
  powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

  # macOS/Linux
  curl -LsSf https://astral.sh/uv/install.sh | sh
  ```

### Установка зависимостей

```bash
# Клонируйте репозиторий
git clone https://github.com/your-username/gh-youtube_downloader.git
cd gh-youtube_downloader

# Установите зависимости
uv sync
```

### Конфигурация

1. Скопируйте файл `.env.example` в `.env`:

   ```bash
   cp .env.example .env
   ```

2. Отредактируйте `.env` и укажите путь для сохранения видео:

   ```
   DOWNLOAD_DIR=/path/to/your/downloads
   ```

   Если не указать, видео будут сохраняться в `./downloads`

## Использование

### Интерактивный режим

Запустите без параметров для пошагового ввода:

```bash
cd src
uv run python downloader/main.py
```

Программа попросит вас:

1. Ввести URL видео
2. Выбрать качество из доступных вариантов

### Режим с параметрами

**С указанием URL и качества:**

```bash
cd src
uv run python downloader/main.py --url "https://youtube.com/watch?v=VIDEO_ID" --quality 720p
```

**Только URL (качество выбирается интерактивно):**

```bash
cd src
uv run python downloader/main.py --url "https://youtube.com/watch?v=VIDEO_ID"
```

### Параметры командной строки

- `--url` — URL видео на YouTube
- `--quality` — качество видео (например: `1080p`, `720p`, `480p`) или номер из списка
- `--help` — справка по использованию

## Примеры

```bash
# Скачать видео в качестве 1080p
cd src
uv run python downloader/main.py --url "https://youtu.be/dQw4w9WgXcQ" --quality 1080p

# Скачать видео, выбрав качество из списка
cd src
uv run python downloader/main.py --url "https://youtu.be/dQw4w9WgXcQ"

# Интерактивный режим
cd src
uv run python downloader/main.py
```

## Структура проекта

```
gh-youtube_downloader/
├── README.md
├── CLAUDE.md              # Инструкции для Claude Code
├── pyproject.toml         # Конфигурация uv и зависимости
├── .env.example           # Пример файла конфигурации
├── .env                   # Локальная конфигурация (не в git)
└── src/
    └── downloader/
        └── main.py        # Основной скрипт
```

## Ограничения

Следующие функции **не включены** в проект:

- Скачивание только аудио
- Скачивание плейлистов
- Авторизация на YouTube
- Конвертация в другие форматы (только MP4)
- Скачивание субтитров

## Обработка ошибок

- ❌ Неверный URL → понятное сообщение об ошибке
- 🌐 Ошибки сети → корректное завершение с сообщением
- 📁 Отсутствует `DOWNLOAD_DIR` → использование `./downloads` по умолчанию
- ⌨️ Ctrl+C → корректный выход с кодом 130
